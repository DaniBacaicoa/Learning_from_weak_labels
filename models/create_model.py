from benchmark_mlp import mlp_feature
from copy import deepcopy

def create_model(confif, **kwargs):
    if config.dt == 'benchmark'
        net = mlp_feature(args['num_features'], args['num_features'], args['num_classes'])
        enc = deepcopy(net)
    return net, enc